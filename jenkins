pipeline {
    agent any
    stages {
       stage('Clean') {
                  steps {
                      echo "Cleaning the code .........."
                      bat "mvn clean"
                  }
              }
        stage('Build') {
            steps {
               echo "Building the code .........."
                      bat "mvn compile"
            }
        }
    }
}
